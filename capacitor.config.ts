import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.speech2text.www',
  appName: 'Speech-to-Text',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
